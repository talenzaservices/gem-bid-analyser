GeM Bid Analyser — local dev instructions.

1. Copy .env.example to .env and edit.
2. Start with Docker Compose:
   docker compose up --build
3. Backend: http://localhost:8000
   Frontend: http://localhost:5173
